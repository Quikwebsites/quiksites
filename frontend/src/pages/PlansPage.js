// This page will contain the different plans available to the user
//   - basic
//   - premium
// It will also include a "proceed to checkout" button which allows them to create their subscription

import React from 'react'

import PricingPage from '../sections/Pricing/PricingPage'

const PlansPage = () => {
  return (
    <div>
        <PricingPage />
    </div>
  )
}

export default PlansPage